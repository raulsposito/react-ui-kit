import styled from 'styled-components'

import { spacing } from '@global/theme'

export const StyledInputCharacterCounter = styled.span`
  pointer-events: none;
  margin: ${spacing.oneAndAHalf} ${spacing.half} ${spacing.half} ${spacing.one};
`
